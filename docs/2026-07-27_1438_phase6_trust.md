# Phase 6 汇报：trust_service/ 信任服务方

**日期**: 2026-07-27 14:38

## 完成内容

| 文件 | 职责 | 状态 |
|---|---|---|
| `database.py` | SQLite 数据库（4 张表） | done |
| `event_registry.py` | 9 个 ACT 事件 + 4 个 demo 事件 | done |
| `record_index.py` | 存证存储、查询、按条件检索 | done |
| `verifier.py` | 存证记录验证（摘要、完整性） | done |
| `trace_service.py` | 按 delegation_id 检索完整证据链 | done |
| `evidence_service.py` | 证据包导出（含完整性判断） | done |
| `anchor_exporter.py` | 锚定摘要生成（教学用途） | done |
| `app.py` | FastAPI（7 个端点） | done |
| `__init__.py` | 统一导出 | done |

## API 端点

```text
POST /v1/attestations
GET  /v1/attestations/{id}
GET  /v1/attestations
POST /v1/attestations/{id}/verify
GET  /v1/traces/{delegation_id}
POST /v1/evidence-packages
POST /v1/anchors/export
GET  /health
```

## 下一步

Phase 7: buyer/ 买方智能体
