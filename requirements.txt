# ============================================================
# ACT智能体自主委托支付Demo — Python依赖
# Python >= 3.10
# ============================================================

# Web框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.6

# 环境变量
python-dotenv>=1.0.0

# 异步HTTP客户端（买方智能体调用各服务）
httpx>=0.27.0

# LLM API调用（DeepSeek兼容OpenAI SDK）
openai>=1.50.0

# 数据验证（FastAPI内置，显式锁定版本）
pydantic>=2.9.0

# 密码学（Ed25519签名、SHA-256摘要）
cryptography>=43.0.0

# 文档处理（DOCX翻译）
python-docx>=1.0.0

# 数据库（SQLite驱动，内置sqlite3的增强封装）
aiosqlite>=0.20.0

# 测试
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0

# HTTP健康检查（run_demo.py 使用）
requests>=2.31.0
